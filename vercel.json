{
    "version": 2,
    "builds": [
      {
        "src": "pages/api/[...route].ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "pages/api/[...route].ts"
      }
    ]
  }